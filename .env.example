# Every value here is optional unless marked required — defaults live in
# config/index.js and match the values shown. Copy to .env.local and edit.

# ── Supabase (required) ───────────────────────────────────
SUPABASE_URL=https://YOUR-PROJECT.supabase.co
SUPABASE_ANON_KEY=YOUR-ANON-KEY
SUPABASE_ENV_TABLE=environment
SUPABASE_FOCUS_TABLE=focus
SUPABASE_TIMEOUT_MS=10000

# ── Flood-warning bridge (StreeFlood project) ─────────────
# Defaults to the same Supabase as above (shared `sensors` + `water_readings`
# tables). Point at the original StreeFlood Supabase to read its live data:
# FLOOD_SUPABASE_URL=https://magnudsyqmyqrxhqbcyx.supabase.co
# FLOOD_SUPABASE_ANON_KEY=<StreeFlood anon key>
# FLOOD_SENSORS_TABLE=sensors
# FLOOD_READINGS_TABLE=water_readings
# FLOOD_TREND_READINGS=24
# FLOOD_TREND_WINDOW_HOURS=3
# FLOOD_STABLE_RATE=0.3
# FLOOD_STALE_AFTER_HOURS=6
# FLOOD_TIMEOUT_MS=10000

# ── StreeFlood app (gov-data proxy + redirect button) ─────
# STREEFLOOD_BASE_URL=https://streeflood.vercel.app
# STREEFLOOD_GOV_PATH=/api/gov
# STREEFLOOD_TIMEOUT_MS=30000

# ── Gemini (optional — leave key empty to run local analysis only) ──
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash
GEMINI_BASE_URL=https://generativelanguage.googleapis.com/v1beta
GEMINI_TIMEOUT_MS=25000
GEMINI_MAX_OUTPUT_TOKENS=1024
# GEMINI_MAX_HISTORY_TURNS=10

# ── API behaviour ─────────────────────────────────────────
API_DEFAULT_HOURS=24
API_MAX_HOURS=168
API_DEFAULT_HISTORY_LIMIT=200
API_MAX_HISTORY_LIMIT=1000
API_DEVICE_SCAN_LIMIT=1000
# API_DEVICE_LOOKBACK_HOURS=168
# API_FALLBACK_ROWS=40

# ── Client defaults (served via /api/config) ─────────────
CLIENT_POLL_MS_DEFAULT=5000
CLIENT_POLL_MS_MIN=3000
# CLIENT_POLL_MS_MAX=60000
# CLIENT_HEALTH_POLL_MS=30000
# CLIENT_FLOOD_REFRESH_MS=60000
# CLIENT_GOV_REFRESH_MS=300000

# ── Focus (webcam) feed ───────────────────────────────────
FOCUS_FETCH_LIMIT=120
FOCUS_POLL_MS=20000
FOCUS_REALTIME_RETRY_MS=15000
FOCUS_THRESHOLD_DEFAULT=8
# FOCUS_REALTIME_HEARTBEAT_MS=25000
# FOCUS_BUCKET_MS=60000
# FOCUS_CHART_BUCKETS=30
